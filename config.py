# This file contains config options.

# the name of the database file to be used
database_file = "minipam.db"

# the address and port the server is listening on
xmlrpc_address = "localhost"
xmlrpc_port = 8000
